Compilar no Linux:
./configure && make

Compilar no MinGW/Windows:
./configure && ./make-mingw-win

Compilar no Linux gerando executável para Windows:
./configure && ./make-mingw-lin

Se souber alguma forma de fazer o autoconf/automake compilar no Windows,
entrar em contato.

