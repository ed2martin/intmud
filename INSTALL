Para compilar (Linux e MinGW/Windows):
./configure && make

Se quiser reduzir o tamanho do executável:
strip --strip-all intmud
    Se for Windows:
strip --strip-all intmud.exe

Compilar no Linux gerando executável para Windows:
./configure && ./make-mingw-lin

